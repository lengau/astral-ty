name: Cachebust
# Make Launchpad re-build the snap daily

on:
  schedule:
    - cron: "0 10 * * *"

permissions:
  contents: write

jobs:
  cachebust:
    runs-on: ubuntu-latest
    steps:
      - run: |
          date > .cachebust
          git commit -m 'chore: cachebust'
          git push
